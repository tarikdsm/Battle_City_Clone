// Placeholder — the ASCII level contact-sheet generator lands in a later task.
// Runs via `npm run levels:preview` (tsx). Prints a notice and exits 0.
console.log('level-preview: not yet implemented');
